language: none

#for docker
build:

  ci:
    - echo "hello"
    - bash test.sh

  
  post_ci:
    - 
    - docker push kashishgrover/node-web-app-1:latest

integrations:
  hub:
    - integrationName: Kashish-Docker
      type: docker
      branches:
        only:
          - master
