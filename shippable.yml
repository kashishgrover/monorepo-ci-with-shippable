language: none

#for docker
build:

  ci:
    - echo "hello"
    - ls -la
    - ./test.sh

integrations:
  hub:
    - integrationName: Kashish-Docker
      type: docker
      branches:
        only:
          - master
