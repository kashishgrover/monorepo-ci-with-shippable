language: none

#for docker
build:

  ci:
    - echo "hello"
    - bash test.sh

integrations:
  hub:
    - integrationName: Kashish-Docker
      type: docker
      branches:
        only:
          - master
