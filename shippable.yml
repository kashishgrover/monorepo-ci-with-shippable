language: none

#for docker
build:

  ci:
    - echo "hello"
    - ls -la
    - ./build.sh

integrations:
  hub:
    - integrationName: Kashish-Docker
      type: docker
      branches:
        only:
          - master
