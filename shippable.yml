language: none

#for docker
build:

  ci:
    - echo "The Magic Happens Here"
    - ./build.sh

integrations:
  hub:
    - integrationName: Kashish-Docker
      type: docker
      branches:
        only:
          - master
